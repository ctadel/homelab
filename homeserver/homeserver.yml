networks:
  homeserver:
    driver: bridge

  theatre:
    driver: bridge

  cloudserver:
    driver: bridge

include:
  - cloudserver/cloudserver.yml
  - homesecurity/homesecurity.yml
  - homeassistant/homeassistant.yml
  - mediaserver/mediaserver.yml

